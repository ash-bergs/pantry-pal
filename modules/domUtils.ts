// This module will provide the sections of UI we build in `populateItems` right now
// We're creating a class, ItemManager, and it doesn't make sense to include these things there
// this will keep UI and Items manipulation decoupled

export const renderItemsList = () => {};

export const renderSectionBubbles = () => {};

export const noItemsMessage = () => {};

// might belong somewhere else, but we only use it for renderSectionBubble right now
export const capitalizeFirstLetter = (string: string) => {};
